const size = {
    gb: '1024px',
    md: '440px',
    sm: '390px'
}

const breakpoints = {
    gb: `(max-width: ${size.gb})`,
    md: `(max-width: ${size.md})`,
    sm: `(max-width: ${size.sm})`,
}

export default breakpoints;