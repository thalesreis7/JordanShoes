const size = {
    // Tablets
    XL: '884px',
    MD: '840px',
    SM: '780px'
    
}

const breakpoints = {
    XL: `(max-width: ${size.XL})`,
    MD: `(max-width: ${size.MD})`,
    SM: `(max-width: ${size.SM})`,
}

export default breakpoints;